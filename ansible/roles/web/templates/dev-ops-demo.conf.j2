server {
    listen 80;
    server_name localhost;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/home/{{app_user}}/{{project_name}}/{{project_name}}.sock;
    }

    location /health/ {
        root /var/www/public_html;
    }
}